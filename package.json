{
  "name": "doc-repo-tools",
  "version": "0.1.0",
  "private": true,
  "description": "OKF document repo tooling (convert, deconstruct)",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "test:coverage": "pnpm -r run test:coverage"
  },
  "devDependencies": {
    "c8": "^10.1.3"
  }
}
